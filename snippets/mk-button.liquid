{%- doc -%}
  MK 按钮组件

  渲染一个按钮链接

  @param {string} href - 链接地址
  @param {string} [label] - 按钮文字
  @param {string} [style] - 样式：primary, secondary, outline
  @param {boolean} [disabled] - 是否禁用
  @param {string} [class] - 额外CSS类名

  @example
  {% render 'mk-button', href: '/collections/all', label: '查看全部', style: 'primary' %}
{%- enddoc -%}

{% liquid
  assign href = href | default: '#'
  assign label = label | default: '按钮'
  assign style = style | default: 'primary'
  assign disabled = disabled | default: false
-%}

<a
  {% if href == blank %}
    role="link"
    aria-disabled="true"
  {% else %}
    href="{{ href }}"
  {% endif %}
  class="mk-button mk-button--{{ style }} {{ class }}"
  {% if disabled %}
    aria-disabled="true"
  {% endif %}
>
  {{ label }}
</a>

{% stylesheet %}
  .mk-button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding: 14px 28px;
    font-size: 14px;
    font-weight: 600;
    text-decoration: none;
    border-radius: 8px;
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .mk-button:focus {
    outline: none;
    box-shadow: 0 0 0 3px rgba(0, 0, 0, 0.1);
  }

  /* Primary 样式 */
  .mk-button--primary {
    color: var(--color-on-primary, #fff);
    background-color: var(--color-primary, #000);
    border: none;
  }

  .mk-button--primary:hover {
    opacity: 0.85;
    transform: translateY(-1px);
  }

  /* Secondary 样式 */
  .mk-button--secondary {
    color: var(--color-foreground);
    background-color: var(--color-background-secondary, #f5f5f5);
    border: none;
  }

  .mk-button--secondary:hover {
    background-color: var(--color-border);
  }

  /* Outline 样式 */
  .mk-button--outline {
    color: var(--color-primary, #000);
    background-color: transparent;
    border: 1px solid var(--color-primary, #000);
  }

  .mk-button--outline:hover {
    color: var(--color-on-primary, #fff);
    background-color: var(--color-primary, #000);
  }

  /* 禁用状态 */
  .mk-button[aria-disabled="true"] {
    opacity: 0.5;
    cursor: not-allowed;
    pointer-events: none;
  }

  /* 尺寸变体 */
  .mk-button--small {
    padding: 10px 20px;
    font-size: 13px;
  }

  .mk-button--large {
    padding: 18px 36px;
    font-size: 16px;
  }

  /* 全宽按钮 */
  .mk-button--full {
    width: 100%;
  }
{% endstylesheet %}
