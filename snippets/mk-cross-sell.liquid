{%- doc -%}
  MK 交叉销售组件

  渲染交叉销售产品

  @param {string} [class] - 额外CSS类名

  @example
  {% render 'mk-cross-sell' %}
{%- enddoc -%}

{% liquid
  assign class = class | default: ''
-%}

<div class="mk-cross-sell {{ class }}" data-cross-sell>
  <h3 class="mk-cross-sell__title">{{ 你可能还喜欢 | default: '你可能还喜欢' }}</h3>

  <div class="mk-cross-sell__products" data-cross-sell-products>
    {%- comment -%} 动态加载交叉销售产品 {%- endcomment -%}
  </div>
</div>

{% stylesheet %}
  .mk-cross-sell {
    padding: var(--mk-spacing-xl) 0;
  }

  .mk-cross-sell__title {
    margin: 0 0 20px 0;
    font-size: var(--mk-font-size-xl);
    font-weight: 600;
    color: var(--color-foreground);
  }

  .mk-cross-sell__products {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--mk-spacing-lg);
  }

  @media (min-width: 750px) {
    .mk-cross-sell__products {
      grid-template-columns: repeat(4, 1fr);
    }
  }
{% endstylesheet %}
